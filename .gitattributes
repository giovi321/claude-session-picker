# Normalize line endings: LF in the repository, native on checkout.
* text=auto eol=lf

# Shell scripts must stay LF so they run on POSIX.
*.sh text eol=lf
# Python is fine with LF everywhere.
*.py text eol=lf
# PowerShell scripts prefer CRLF on Windows.
*.ps1 text eol=crlf
